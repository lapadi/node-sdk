import { CheckoutRequest, SessionRequest, LapadiRequestConfig } from "../helpers/constants";
import { CacheModule } from "../modules";
declare class ShopService {
    cartId: string;
    cache: CacheModule;
    count: number;
    total: number;
    items: any[];
    localCart: any;
    lapadi_api_endpoint: string;
    lapadi_user_token: string;
    lapadi_app_token: string;
    session_data: any;
    constructor(config: LapadiRequestConfig);
    checkCart(): void;
    getItems(): Promise<any[]>;
    setItems(items: any): Promise<void>;
    clearItems(): Promise<void>;
    addItem(item: any): Promise<void>;
    incresItem(item: any): Promise<void>;
    decreaseItem(item: any): Promise<void>;
    containsItem(id: string): boolean;
    updateItem(object: any): Promise<unknown>;
    saveCart(objects: any): Promise<unknown>;
    getCart(): any;
    clearCart(): void;
    session(data: SessionRequest): Promise<unknown>;
    checkout(data: CheckoutRequest): Promise<any>;
}
export { ShopService };
